<%= form_with(model: design, method: :delete, local: true) do |form| %>
  <div class="modal-header">
    <h5 class="modal-title" id="Title">Confirmar borrado</h5>
    <button type="button" class="close text-light" data-dismiss="modal" aria-label="Close">
      <span aria-hidden="true">&times;</span>
    </button>
  </div>
  <div class="modal-body">
    <div class="form-group">
      Para borrar el diseño <strong><%= @design.name %></strong> de los <%= @design.house.name %>, por favor escribe "DELETE" en el siguiente campo:
    </div>
    <div class="input-group mb-3">
      <div class="input-group-prepend">
        <div class="input-group-text"><i class="fas fa-trash-alt"></i>&nbsp;</div>
      </div>
      <%= form.text_field :confirm, class: "form-control" %>
    </div>
    <div class="input-group mb-3">
      <%= form.submit 'Eliminar diseño', class: "btn btn-primary" %>
    </div>
    <div class="input-group mb-3">
      Este diseño está en uso en las siguientes naves:
    </div>
  </div>
<% end %>
